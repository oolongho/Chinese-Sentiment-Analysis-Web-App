# 毕业设计工作记载册

## 项目名称：基于深度学习与情感词典的中文情感分析Web平台

| 周次 | 时间 | 设计进展概述 | 遇到的疑难问题 | 思考讨论和改进意见 |
|------|------|-------------|---------------|-------------------|
| 第1周 | 2.19-2.25 | 完成项目整体架构设计，搭建前后端开发环境。后端采用FastAPI框架，前端采用React+TailwindCSS技术栈；编写项目配置文件config.py，定义数据路径、环境变量、数据库URL及训练参数等基础配置；完成情感词典数据的收集与整理，包括基础情感词表、否定词表和程度副词表的构建。 | 情感词典数据来源分散，不同词典的情感极性标注标准不一致，直接合并会导致评分冲突；FastAPI项目结构缺乏统一规范，路由与服务层划分不清晰。 | 对多个情感词典进行对齐处理，统一情感极性评分标准，采用归一化方法消除量纲差异；参考FastAPI最佳实践，将项目划分为routers（路由层）、services（服务层）、sentiment（分析模块层）三层架构，明确职责边界。 |
| 第2周 | 2.26-3.4 | 完成情感词典分析器（lexicon_analyzer.py）的核心开发，实现基于情感词典的文本情感分析功能，包括情感词匹配评分、否定词翻转处理、程度副词加权增强以及特殊搭配模式检测（如"不+积极词"等复合模式）；编写词典加载与缓存机制，支持多词典融合评分。 | 否定词处理逻辑复杂，"不不好"等双重否定场景容易误判；程度副词的加权系数缺乏理论依据，不同副词对情感强度的调节幅度难以量化；特殊搭配模式覆盖不全，部分口语化表达无法正确识别。 | 引入否定词作用域机制，以标点符号和句法边界限定否定词的影响范围，避免过度翻转；参考文献中程度副词的分级标准，将副词划分为4个等级并赋予经验加权系数；逐步扩充特殊搭配模式库，优先覆盖高频口语表达。 |
| 第3周 | 3.5-3.11 | 完成深度学习模型分析器（model_analyzer.py）的开发，基于RoBERTa-wwm-ext中文预训练模型实现文本情感分类；完成模型的微调训练流程，包括数据预处理、训练参数配置与模型保存；搭建模型推理接口，支持批量文本的情感预测与置信度输出。 | RoBERTa模型文件较大（约400MB），首次加载耗时较长，影响API响应速度；微调训练时显存占用过高，batch_size受限导致训练效率低；部分短文本情感预测置信度偏低，模型对含隐含情感的句子判别能力不足。 | 采用延迟加载策略，应用启动时预加载模型至内存，后续请求直接调用；使用梯度累积技术模拟大batch_size训练效果，在显存受限条件下保证训练稳定性；对低置信度样本引入词典辅助判断，为后续混合策略开发奠定基础。 |
| 第4周 | 3.12-3.18 | 完成混合情感分析器（hybrid_analyzer.py）的设计与实现，提出五种混合推理策略：级联加速策略（词典预筛选后模型精判）、置信度加权策略（根据各分析器置信度动态融合）、规则修正策略（词典规则修正模型输出）、增强级联策略（多级筛选逐步提升精度）、自适应路由策略（根据输入特征自动选择最优策略）；实现策略配置与统计分析功能。 | 五种策略的适用场景难以界定，不同策略在不同文本类型上表现差异大；置信度加权策略中词典与模型的置信度尺度不一致，直接加权融合效果不佳；自适应路由策略的特征选择和决策边界设计缺乏参考依据。 | 设计对比实验框架，在标准数据集上评估各策略的准确率和推理速度，根据实验结果明确各策略的适用场景；对词典和模型的置信度分别进行归一化处理，映射到统一尺度后再加权融合；采用基于文本长度、情感词密度和句式复杂度的多维特征构建路由决策函数。 |
| 第5周 | 3.19-3.25 | 完成前端核心页面开发，包括文本分析页面（TextAnalysisPage）和音频分析页面（AudioAnalysisPage）；实现音频分析后端功能，基于FunASR框架完成语音识别服务（speech_service.py），支持音频文件上传、语音转文字及情感分析全流程；编写API配置模块（api.ts），统一前后端接口对接。 | FunASR模型加载依赖较大运行环境，部署配置复杂；音频文件格式多样，部分格式FunASR无法直接处理；前端音频上传组件的进度反馈不够直观，大文件上传时用户体验差。 | 编写自动化部署脚本，封装FunASR环境依赖，简化安装流程；在后端增加音频格式预处理模块，统一转换为WAV格式后再送入识别引擎；为上传组件增加分片上传和实时进度条显示，提升大文件上传体验。 |
| 第6周 | 3.26-4.1 | 完成模型评估模块（evaluation.py）开发，支持数据上传、评估运行、结果查询、错误样本查看和结果导出等功能；实现消融实验模块（hybrid_experiment.py），支持词典分析、深度学习模型和混合方法三种方案的性能对比；对情感词典进行增强，扩充特殊搭配模式库和程度副词词表。 | 消融实验结果显示部分混合策略在特定数据集上不如纯深度学习方法，策略组合并非总能带来提升；评估过程中数据集格式不统一，频繁出现解析错误；错误样本分析发现模型对反讽和隐喻类文本判别能力弱。 | 调整混合策略的触发条件，仅在词典置信度高于阈值时启用规则修正，避免低质量词典判断干扰模型输出；制定统一的数据集格式规范，增加格式校验和自动转换功能；引入反讽检测预处理模块，对疑似反讽文本标记并降低模型置信度。 |
| 第7周 | 4.2-4.8 | 完成模型量化服务开发，实现FP32、FP16和INT8三种量化模式（quantization_service.py），支持模型加载、量化转换和显存监控；完成ONNX量化服务（onnx_quantization_service.py），实现PyTorch模型到ONNX格式的导出及INT8量化；开发前端量化功能界面（QuantizationContent），支持不同量化精度的对比展示；完成性能基准测试脚本（performance_benchmark.py）。 | INT8量化后模型精度下降明显，部分情感类别的F1值下降超过5%；ONNX模型导出过程中RoBERTa的注意力层算子兼容性问题导致转换失败；量化模型与原始模型的推理结果存在细微差异，需要建立精度评估标准。 | 采用量化感知训练（QAT）思路，在微调阶段引入量化噪声模拟，提升模型对低精度的适应能力；针对不兼容算子编写自定义ONNX算子映射，手动替换注意力层实现；制定量化精度容忍度标准（F1下降不超过2%），对超标的量化方案回退至FP16。 |
| 第8周 | 4.9-4.15 | 完成系统整体集成测试，验证前后端交互流程及各功能模块的协同工作；修复测试中发现的接口联调问题和边界情况处理缺陷；完成系统安全配置验证，确保API密钥和数据库连接等敏感信息通过环境变量管理；整理项目文档和代码注释，完善部署说明。 | 集成测试中发现音频分析与文本分析的并发请求会导致显存溢出；部分API接口缺少输入校验，异常输入导致服务崩溃；部署文档与实际环境存在偏差，部分依赖版本未明确指定。 | 引入请求队列和并发控制机制，限制同时加载的模型实例数量，避免显存溢出；为所有API接口增加参数类型校验和异常捕获，返回标准化错误信息；重新梳理部署流程，锁定所有依赖版本号，编写一键部署脚本确保环境一致性。 |
